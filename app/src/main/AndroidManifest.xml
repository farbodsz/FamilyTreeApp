<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.familytreeapp">

    <application
        android:name=".FamilyTreeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.TreeActivity" />

        <activity android:name=".ui.TreeListActivity" />

        <activity
            android:name=".ui.PersonListActivity"
            android:label="@string/title_person_list" />

        <activity
            android:name=".ui.ViewPersonActivity" />

        <activity
            android:name=".ui.EditPersonActivity"
            android:label="@string/title_edit_person" />
    </application>

</manifest>
